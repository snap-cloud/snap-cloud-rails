This directory contains Snap! overrides or additions to the PureCSS framework.

All files in this directory are automatically built and combined into the main
application.css file, after the main pure-min file is loaded. To keep things
manageable, use small isolated CSS files.